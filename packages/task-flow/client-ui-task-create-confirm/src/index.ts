/**
 * Task-creation confirmation card, node half. Deliberately empty: the card
 * renders in the browser through the keyed tool.call.toolview seat; creation
 * routes through the tasks Remote.
 */

/** Host plugin body — the confirm card is a pure browser surface. */
export function apply(): void {}
